const faculty_home = require('./faculty_home'),
      faculty_info = require('./faculty_info'),
      update_info = require('./update_info'),
      selection_info = require('./selection_info'),
      reserve_slot = require('./reserve_slot');

module.exports = {
  faculty_home,
  faculty_info,
  update_info,
  selection_info,
  reserve_slot
}